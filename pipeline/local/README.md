This set of scripts can be used to run a pipeline locally.

Following is required:
 - Following executables in the path.
 -- bwa
 -- sambamba
 -- picard
 -- gatk
 -- fastqc
 - GATK resource bundle
 - Reference genome (indexed)