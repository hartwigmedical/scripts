#!/usr/bin/env bash

source message_functions || exit 1

patient=$1

if [[ -z ${patient} ]]; then
    error "No patient provided"
fi

gcloud compute scp data-vm-prod-1:/data/actin/samples/*${patient}*/orange/*.pdf ~/hmf/tmp/