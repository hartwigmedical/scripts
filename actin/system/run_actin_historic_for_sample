#!/usr/bin/env bash

source message_functions || exit 1

sample=$1 && shift

if [[ -z "${sample}" ]]; then
    error "Parameters missing. Exiting.";
fi

echo "Running ACTIN in historic mode"

actin_treatment_matcher_pilot ${sample} -run_historically

echo ""

load_actin_treatment_match_pilot ${sample}