#!/usr/bin/env bash

for i in {2,3}; do
    echo "--- Checking crunch${i} -----"
    ssh crunch00${i} check_samplesheets | grep -vP "^\s*$" | grep -v "^#" | sort -k2
done

echo "[INFO] Crunch001 removed -> add back once up and running again"