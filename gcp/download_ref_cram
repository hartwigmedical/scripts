#!/usr/bin/env bash

source api_functions

set=$1 && shift

if [[ -z "${set}" ]]; then
    echo "[ERROR] No set provided. Exiting."
    exit 1
fi

biopsy=$(load_ref_sample_id_for_set "${set}")
sample="${biopsy}"

do_download_cram_for_sample "${biopsy}" "${set}" "${sample}"