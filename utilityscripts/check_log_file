#!/usr/bin/env bash

fileName=$1

if [ -z "fileName" ]; then
    echo "[ERROR] No log file name. Exiting"
    exit 1
fi

cat ${fileName} | grep -Pi "error|excep|fail" | grep -Pv "^#.*(E|e)rrors :.*\(" | grep -Pv "errorCorrection|OnFirstError|download:"