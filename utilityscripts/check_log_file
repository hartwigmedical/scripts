#!/usr/bin/env bash

file_name=$1

if [ -z "${file_name}" ]; then
    echo "[ERROR] No log file name. Exiting"
    exit 1
fi

cat ${file_name} | grep -i "error|excep|fail" | grep -v "^#.*(E|e)rrors :.*\(" | grep -v "errorCorrection|OnFirstError|download:"