#!/bin/bash
runs_dir=/data/cpct/runs

for file in ${runs_dir}/*; do
    sample=$(ls ${file} | grep -E '^(CPCT|DRUP)[0-9]{8}[^R]')
    if [ $? -eq 0 ]
    then
    	echo "Processing sample" ${sample}
    	# Disabled for the moment
    	#do_apply_somatic_variant_pon $file
    	#do_load_somatic_variants $file
    fi
done
