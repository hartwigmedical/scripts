#!/usr/bin/env bash

source locate_files

prefix=$(dirname $(readlink $0 || echo $0))

credentials=$(locate_pilot_database_credentials)
patient_db_jar=$(locate_pilot_patient_db)

${prefix}/do_load_canonical_transcripts ${credentials} ${patient_db_jar}