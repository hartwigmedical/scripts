#!/usr/bin/env bash

source locate_files || exit 1

run_dir=$1 && shift
output_dir=${run_dir}/linx/

database="hmfpatients"
linx_jar=$(locate_prod_linx)

do_run_linx ${run_dir} ${database} ${linx_jar} ${output_dir} "$@"
