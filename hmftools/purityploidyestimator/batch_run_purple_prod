#!/bin/bash
starts_with=$1 && shift

for file in /data/cpct/runs/*; do
    if [[ $file == /data/cpct/runs/${starts_with}* ]]; then
        do_run_purple_prod $file $@
        do_load_somatic_variants_prod $file
        do_run_sv_loader_prod $file
    fi
done