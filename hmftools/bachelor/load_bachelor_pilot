#!/usr/bin/env bash

source locate_files

prefix=$(dirname $(readlink $0 || echo $0))

run_dir=$1 && shift
credentials=$(locate_pilot_database_credentials)
bachelor_jar=$(locate_pilot_bachelor)

${prefix}/do_load_bachelor_data ${run_dir} ${credentials} ${bachelor_jar} "$@"
