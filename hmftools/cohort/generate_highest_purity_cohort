source locate_files

out=$1 && shift
credentials=$(locate_prod_database_credentials)
paddle_jar=/data/common/tools/paddle_pilot/paddle.jar

# Database details
db_user=$(awk '/^user/ {print $2}' ${credentials})
db_pass=$(awk '/^pass/ {print $2}' ${credentials})
db_url=$(awk '/^url/ {print $2}' ${credentials})

echo "[INFO] Generating highest purity cohort"

java -Dorg.jooq.no-logo=true -cp ${paddle_jar} com.hartwig.hmftools.paddle.PaddleCohortApplicationKt \
    -out $out \
    -db_user ${db_user} -db_pass ${db_pass} -db_url ${db_url} \
    "$@"