#!/usr/bin/env bash

dir_with_runs=$1 && shift

if [[ -z "${dir_with_runs}" ]]; then
    echo "[ERROR] No dir with runs are provided. Exiting" && exit 1;
fi

for run in ${dir_with_runs}*; do
    echo "[INFO] Create patient report for ${run}"
    #create_patient_report_for_run ${run}
done