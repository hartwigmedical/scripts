#!/bin/bash

prefix=`dirname $(readlink $0 || echo $0)`

"$prefix"/do_run_base_patient_reporter \
    -not_sequenceable \
    -not_sequenceable_reason low_tumor_percentage \
    -not_sequenceable_sample "$@"