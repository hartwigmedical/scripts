#!/bin/bash

bucket=$1
set=$2
path=/data/schuberg/fromweekly
download_set_partially_from_bucket -b ${bucket} -s ${set} -l ${path}
db_execute do_run_purple_experiment ${path}/${set}
db_execute do_load_somatic_variants ${path}/${set}